{
  "name": "tools",
  "version": "1.0.0",
  "type": "module",
  "description": "Collection of useful web tools",
  "scripts": {
    "dev": "wrangler dev",
    "build": "npx tsx src/ssg.ts",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:screenshot": "playwright test tests/screenshot.spec.ts"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251225.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "@vitest/coverage-v8": "^4.0.16",
    "serve": "^14.2.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16",
    "wait-on": "^9.0.3",
    "wrangler": "^4.54.0"
  }
}
