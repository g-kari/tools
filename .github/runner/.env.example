# GitHub Actions Self-Hosted Runner Configuration
# Copy this file to .env and fill in your values

# Required: GitHub repository in format owner/repo
GITHUB_REPOSITORY=g-kari/tools

# Required: GitHub Personal Access Token
# For private repos: requires 'repo' scope
# For public repos: requires 'public_repo' scope
# For organization runners: requires 'admin:org' scope
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Optional: Runner name (defaults to hostname)
RUNNER_NAME=tools-e2e-runner

# Optional: Runner labels (comma-separated)
# Default: self-hosted,Linux,X64,playwright,e2e
RUNNER_LABELS=self-hosted,Linux,X64,playwright,e2e

# Optional: Runner group (for organizations)
RUNNER_GROUP=Default

# Optional: Work directory inside container
RUNNER_WORK_DIR=_work
